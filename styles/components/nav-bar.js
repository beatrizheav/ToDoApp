import { StyleSheet } from "react-native";

export const navBar = StyleSheet.create({
  container: {
    width: "100%",
    // backgroundColor: "red",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-around",
  },
});
